title: signal-archive
description: "Tracking anomalies in chatbot behavior during FritzAndFriends streams"
baseurl: "/signal-archive"
url: "https://fritzandfriends.github.io"

theme: minima

future: true

permalink: /:title/

# No author info — the observer remains unnamed
header_pages:
  - about.md

plugins:
  - jekyll-feed

defaults:
  - scope:
      path: ""
    values:
      image: /signal-archive/assets/og-image.png
